using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using TMPro; 
using UnityEngine.UI;

public class TranslatedInventory : MonoBehaviour
{
    public string[] knownDictionary;
    public string[] knownDictionaryTranslation; 
    public string[] allDictionary; 

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
